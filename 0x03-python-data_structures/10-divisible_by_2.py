#!/usr/bin/python3
# 10-divisible_by_2.py


def divisible_by_2(my_list=[]):
    """ function that finds all multiples of 2 in a list."""
    reslut = []
    for nim in my_list:
        if num % == 0:
            result.append(True)
        else:
            result.append(False)
            return (result)
